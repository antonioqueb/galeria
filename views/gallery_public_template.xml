<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Layout Base Independiente (Dark Mode) -->
    <template id="gallery_layout_headless">
        &lt;!DOCTYPE html&gt;
        <html lang="es">
            <head>
                <meta charset="utf-8"/>
                <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1"/>
                <title t-esc="title or 'Galería de Placas'"/>
                
                <!-- Google Fonts -->
                <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&amp;display=swap" rel="stylesheet"/>
                <!-- FontAwesome para iconos -->
                <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css"/>
                
                <!-- CSS de Odoo y del Módulo -->
                <t t-call-assets="web.assets_frontend" t-js="false"/>
                
                <style>
                    body {
                        margin: 0; padding: 0;
                        background-color: #121212;
                        color: #e0e0e0;
                        font-family: 'Inter', sans-serif;
                        overflow-x: hidden;
                    }
                    a { text-decoration: none; }
                    * { -webkit-tap-highlight-color: transparent; }
                    img { display: block; max-width: 100%; }
                </style>
            </head>
            <body>
                <main>
                    <t t-out="0"/>
                </main>
                <!-- JS de Odoo y del Módulo (Aquí se carga gallery_public.js automáticamente) -->
                <t t-call-assets="web.assets_frontend" t-css="false"/>
            </body>
        </html>
    </template>

    <!-- Vista Principal -->
    <template id="gallery_public_view">
        <t t-call="galeria.gallery_layout_headless">
            <t t-set="title" t-value="share.name"/>
            
            <!-- Inyectar Configuración para JS (Antes de que carguen los assets) -->
            <script>
                window.galleryConfig = {
                    token: "<t t-esc="token"/>"
                };
            </script>

            <div class="gallery-wrapper">
                <!-- Header Fijo con Botón de Carrito -->
                <header class="gallery-header">
                    <div class="container-fluid">
                        <div class="header-content">
                            <div class="brand">
                                <span class="logo-text">STONE<span class="light">GALLERY</span></span>
                            </div>
                            
                            <div class="header-actions">
                                <div class="client-info">
                                    <span class="label">Hola,</span>
                                    <span class="client-name" t-field="share.partner_id.name"/>
                                </div>
                                <button id="cart-toggle" class="btn-cart">
                                    <i class="fa fa-shopping-bag"></i>
                                    <span id="cart-count" class="badge">0</span>
                                </button>
                            </div>
                        </div>
                    </div>
                </header>

                <!-- Contenedor Principal -->
                <div class="container content-container">
                    <t t-foreach="grouped_images" t-as="category">
                        <div class="category-block">
                            <h2 class="category-title"><t t-esc="category"/> <span class="line"></span></h2>
                            
                            <div class="bento-grid">
                                <t t-foreach="grouped_images[category]" t-as="img">
                                    <!-- Tarjeta Bento con Data Attributes para JS -->
                                    <div class="bento-item" 
                                         t-attf-class="#{'bento-large' if img['is_large'] else ''}"
                                         t-att-data-id="img['id']"
                                         t-att-data-quant-id="img['quant_id']"
                                         t-att-data-name="img['name']"
                                         t-att-data-lot="img['lot_name']"
                                         t-att-data-dims="img['dimensions']"
                                         t-att-data-area="img['area']"
                                         t-att-data-url="img['url']">
                                        
                                        <div class="bento-card">
                                            <div class="img-container">
                                                <img t-att-src="img['url']" loading="lazy" t-att-alt="img['lot_name']"/>
                                                
                                                <!-- Overlay acciones (Expandir) -->
                                                <div class="card-actions">
                                                    <!-- Botón Expandir (Onclick eliminado, JS detecta .btn-expand) -->
                                                    <button class="btn-expand" type="button">
                                                        <i class="fa fa-expand"></i>
                                                    </button>
                                                </div>
                                            </div>
                                            
                                            <!-- Footer con Info y Botón + -->
                                            <div class="card-footer">
                                                <div class="info-text">
                                                    <span class="product-name" t-esc="img['name']"/>
                                                    <div class="meta">
                                                        <span class="lot">Lote: <t t-esc="img['lot_name']"/></span>
                                                        <span class="sep">•</span>
                                                        <span class="dims" t-esc="img['dimensions']"/>
                                                    </div>
                                                    <span class="area-badge"><t t-esc="img['area']"/> m²</span>
                                                </div>
                                                <!-- Botón Agregar (Onclick eliminado, JS detecta .btn-add-cart) -->
                                                <button class="btn-add-cart" type="button">
                                                    <i class="fa fa-plus"></i>
                                                </button>
                                            </div>
                                        </div>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </t>
                    
                    <div t-if="not grouped_images" class="empty-state">
                        <p>No hay imágenes disponibles en este momento.</p>
                    </div>
                </div>
            </div>

            <!-- COMPONENTS: LIGHTBOX & CART -->
            
            <!-- 1. Lightbox con Zoom (Uso de entity segura para X) -->
            <div id="lightbox" class="lightbox">
                <!-- Botón cerrar (JS detecta .close-lightbox) -->
                <button class="close-lightbox" type="button">&#215;</button>
                <div class="lightbox-content" onmousemove="gallery.zoomImage(event)" onmouseleave="gallery.resetZoom()">
                    <img id="lightbox-img" src=""/>
                </div>
            </div>

            <!-- 2. Sidecar Cart -->
            <div id="cart-sidebar" class="cart-sidebar">
                <div class="cart-header">
                    <h3>Mi Selección</h3>
                    <button class="close-cart" type="button">&#215;</button>
                </div>
                <div class="cart-items" id="cart-items-container">
                    <!-- JS inyectará items aquí -->
                </div>
                <div class="cart-footer">
                    <div class="totals">
                        <div class="row">
                            <span>Placas:</span>
                            <strong id="total-plates">0</strong>
                        </div>
                        <div class="row">
                            <span>Total m²:</span>
                            <strong id="total-area">0.00</strong>
                        </div>
                    </div>
                    <button id="btn-confirm" class="btn-checkout" type="button">
                        Confirmar Reserva
                    </button>
                </div>
            </div>
            <div id="cart-overlay" class="cart-overlay"></div>
        </t>
    </template>

    <!-- Error Templates -->
    <template id="gallery_not_found">
        <t t-call="galeria.gallery_layout_headless">
            <div class="error-page">
                <h1>404</h1>
                <p>Catálogo no encontrado</p>
            </div>
        </t>
    </template>

    <template id="gallery_expired">
        <t t-call="galeria.gallery_layout_headless">
            <div class="error-page">
                <h1>Enlace Expirado</h1>
                <p>Este catálogo ya no está disponible.</p>
            </div>
        </t>
    </template>
</odoo>